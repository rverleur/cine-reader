# MATLAB Runtime Unpack Libraries

This folder contains only runtime shared libraries used by `Matlab/Cine.m` for packed 10-bit decode:

- `unpack_data_win64.dll`
- `unpack_data_win32.dll`
- `unpack_data_arm64.dylib`
- `unpack_data_elf64.so`

Canonical C source files live in `c_src/`.
